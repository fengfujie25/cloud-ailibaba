server:
  port: 9001

spring:
  application:
    name: provider-server
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848 # nacos地址
#监控
management:
  endpoints:
    web:
      exposure:
        include: '*'
  endpoint:
    health:
      show-details: ALWAYS

